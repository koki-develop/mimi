version: "1.0"

defaults:
  base_dir: ../worktrees/mimi

hooks:
  post_create:
    - type: command
      command: lefthook install
    - type: copy
      from: transcribe/.claude
      to: transcribe/.claude
    - type: copy
      from: app/.claude
      to: app/.claude
